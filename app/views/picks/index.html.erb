<h1>Listing Picks for Week <%= @week %></h1>

<table>
  <tr>
    <th>Week</th>
    <th>Team</th>
    <th>Count</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @picks.each do |pick| %>
  <tr>
    <td><%= pick.week %></td>
    <td><%= pick.team %></td>
    <td><%= pick.count %></td>
    <td><%= link_to 'Show', pick %></td>
    <td><%= link_to 'Edit', edit_pick_path(pick) %></td>
    <td><%= link_to 'Destroy', pick, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pick', new_pick_path %>
